# Rails sample app using Vue.js

## Set up
`git clone git@github.com:keisukefunatsu/rails-vue-data-binding-sample.git`
`bundle install`
`yarn install`

## Run
`foreman start`
Then app starts at //localhost:3001
